

it loads two photo urls 
it have 1 main bloc ad two sub blocs
top bloc and bottom bloc 
one bloc does a heavy lifting  fro there we crate
two blocs concrete instances one is the top bloc 
the other is bottom bloc and then we will have two 
views or widgets these two widgets hooks them self
with these two blocs 